------------------------------------------------------------------------
-- UMST-Formal: Gate.agda (compat shim)
--
-- Re-exports the legacy `Gate` module API from `Compat.Gate`.
------------------------------------------------------------------------

module Gate where

open import Compat.Gate public
