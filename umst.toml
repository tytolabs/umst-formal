# Self-description manifest (blueprint §18 / NEW_REPOS_BUILD_SPEC C8).
# Consumed by umst-meta when implemented; not checked yet.

name = "tytolabs/umst-formal"
path = "umst-formal"

kind = "formal"
spdx = "MIT"
layer = "spine"
formal_anchors = ["physicalSecondLaw", "sequentialCompositionSafe"]
status = "wip"
stability = "evolving"
semver = "0.1.0"

# R4-U1 flag coverage — every manifest carries the compose pair.
composes_into = "self"
composes_into_satisfied = true
composes_note = "R4-U1: already in final position (self)."

[registry]
id = "tytolabs/umst-formal"
validation_status = "unvalidated"
