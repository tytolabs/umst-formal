/-
  Placeholder only — **not** a `lakefile.lean` root.

  Intended future role: boundary types for a gated experiment monad (Rust harness + policy
  must freeze first). See `FORMAL_FOUNDATIONS.md` and private application-runtime continuum backlog (§17.5).
-/

/-- Trivial stub: no automation ships from this file. -/
def AutoExperimenterPlaceholder : Unit := ()
